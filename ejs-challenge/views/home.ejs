<%- include("partials/header") %>
<!-- as we are putting these header and footer into a folder called partials in views we have to add partials/ 
     other wise it is not needed here and only header can be given -->
<h1>HOME</h1>
<p>
    <%= keyword1%>
</p>
<!-- now using for each loop for doing the task -->   
<%posts.forEach(function(post){%>
    <h1><%=post.title%></h1>
    <p><%=(post.body).substring(0,100)+" ..."%>
        <a href="/posts/<%=post.title%>">Read more</a>
    </p>
<%});%>
<%- include("partials/footer") %>

<!-- 
    <%for(var i=0;i<posts.length;i++){%>
    <%console.log(posts[i].title);%>
    <%}%>   
 -->

